<div id="block" class="container" style="padding: 0.8rem;">
    <div class="container" style="text-align: center;">
      <h1>Student Information</h1>
    </div>
    <table class="table table-bordered  text-center " style="margin:auto; margin-bottom: 20px;">
      <thead>
        <tr>
          <th> Roll Number</th>
          <th> Student_name </th>
          <th> Programme </th>
          <th> Discipline </th>
          <!-- <th> Year of Graduation </th> -->
        </tr>
      </thead>
      <tbody>
        <?php
        $link = mysqli_connect('localhost', 'root', '', 'iiitdmj');
        /*Check link to the mysql server*/
        if (!$link) {
          die('Failed to connect to server: ');
        }
        /*Create query*/
        $qry = 'SELECT * FROM students WHERE programme="Phd" ORDER BY name';

        /*Execute query*/
        $result = mysqli_query($link, $qry);

        /*Show the rows in the fetched result set one by one*/
        while ($row = mysqli_fetch_assoc($result)) {
          echo '<tr> 
          <td>' . $row['roll_number'] . '</td>
          <td>' . $row['name'] . '</td>
          <td>' . $row['programme'] . '</td> 
          <td>' . $row['discipline'] . '</td>
          </tr>';
        }
        echo '</tbody>     
    </table>';
        ?>
  </div>